#!/bin/bash
selected=$(tmux list-sessions -F '#{session_name}' 2>/dev/null | fzf --reverse --multi --prompt="kill session> ")
[[ -z "$selected" ]] && exit 0

echo "$selected" | while read -r session; do
  tmux kill-session -t "$session"
done
